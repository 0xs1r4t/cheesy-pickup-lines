# best way to run this would be to create a whole environment for it
python -m venv my_env
source my_env/bin/activate
pip install requirements.txt
python main.py

# you can also run it via docker!
# i'll add the commands for that over here later

